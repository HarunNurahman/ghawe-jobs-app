import 'package:flutter/material.dart';

class MyApplicantScreen extends StatelessWidget {
  const MyApplicantScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('My Applicant Screen'),
      ),
    );
  }
}
