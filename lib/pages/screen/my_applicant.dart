import 'package:flutter/material.dart';

class MyApplicant extends StatelessWidget {
  const MyApplicant({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('My Applicant')),
    );
  }
}
